import './bootstrap';
// Import Bootstrap's JavaScript |  Bootstrap core JavaScript and Popper.js (used for tooltips and popovers). | .bundle.min version includes Bootstrap’s core JS functionality along with the Popper.js library, which is necessary for certain Bootstrap components.
import 'bootstrap/dist/js/bootstrap.bundle.min';

// Import Bootstrap 
import 'bootstrap/dist/css/bootstrap.min.css';

// Import Font Awesome's CSS
import '@fortawesome/fontawesome-free/css/all.min.css';